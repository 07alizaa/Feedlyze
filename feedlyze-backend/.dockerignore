# Dependencies (installed inside container)
node_modules

# Environment & secrets
.env
.env.*
*.pem
*.key

# Git & metadata
.git
.gitignore

# Tests & docs (run in CI, not in runtime image)
tests
test
__tests__
docs
*.md
coverage
.json


# Logs
npm-debug.log
yarn-error.log

# Build output (if any)
dist
build

# Docker files (not needed inside image)
Dockerfile
docker-compose.yml
